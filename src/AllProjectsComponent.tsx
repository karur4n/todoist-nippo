import * as React from 'react'
import { AppConsumer } from "./Root"

export default class AllProjectsComponent extends React.Component {
  render() {
    return (
      <AppConsumer>
      </AppConsumer>
    )
  }
}

